```{prql, file="simple.prql", lang="prql"}
```

```{r}
#| echo: false

library(DBI)
con <- dbConnect(RSQLite::SQLite(), ":memory:")
dbWriteTable(con, "mtcars", mtcars)
```

```{prql}
#| connection: con
#| lang: foo
#| file: simple.prql
```

```{r, echo=FALSE}
dbDisconnect(con)
```
